# -*- coding: utf-8 -*-
# vim: ft=jinja

{## Start with defaults from defaults.yaml ##}
{% import_yaml 'memcached/defaults.yaml' as default_settings %}

{## Merge in memcached pillar ##}
{% set memcached = salt['pillar.get'](
        'memcached',
        default=default_settings.memcached,
        merge=True
    )
%}
